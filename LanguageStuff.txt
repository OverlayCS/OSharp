==========================
   MyLanguage Reference
==========================

this language is an interpreted language 

FUNCTIONS
---------
Function prefix is the return type (or 'void' if it returns nothing).

Examples:

    <visibility> <type> FunctionName(name: type, age: type)
    {
        Console.Message(name + age);
    }

    int FunctionName(age: type)
    {
        Console.Message(age.ToUsableString());
        return age++;
    }


IMPORTING FILES
---------------

use a file in the same director just by using the keyword
	use <FileName>;

or if its not inside the same directory 
	use <Full Path To File>;

or inside a os.config file you can add the 
	{
		IncludeDirectory: "FilePath",
	}
Example:
	{
		IncludeDirectory: "C:\Desktop\Files\" // must add \ at the end or it will throw an error on start
	}


LANGUAGE CONFIG FILE
--------------------
This is a file read on start and set up common stuff that the language can use either dont add stuff to it or keep it blank it wont be read 

to Force Read It On Start Add This One Line

	ConfigFile.ForceRead(<string> PathToFile); // keep blank it will look for it in the current directory
	
All Option For Config File

	// still working on will update

VARIABLES
---------
Declare variables with or without default values.

    <visibility> <type> VariableName;
        // Default initialized to type's empty value (0 for numbers, "" for strings)

    <visibility> <type> VariableName = <default value>;
        // Explicit default value


FUNCTION VISIBILITY
-------------------
    public    // Can be accessed from any class with an object reference
    private   // Accessible only within the defining class
    static    // Belongs to the class itself, no object needed
    internal  // Accessible only to other internal classes


THREADS
-------
Thread Types:
    safe   // Can only access safe, managed memory
    unsafe // Can access any memory used by the program (dangerous)
    io     // For file I/O operations
    net    // For networking operations
    all    // Combines all thread types (not recommended unless necessary)

Up to two thread types may be specified.

Example:

    // Creating a thread
    <visibility> <threadType> <threadType> parallel myThread = new();

    // Starting thread
    myThread.Start()
    {
        // Code executed on this thread
    };

    // Subscribing to thread events
    myThread.OnStarted(-> FunctionThatSubscribes)   
    myThread.OnEnded(-> FunctionThatSubscribes)     
    myThread.OnDestroyed(-> FunctionThatSubscribes) 
	
	// Unsubscribing to thread events
    myThread.OnStarted(<- FunctionThatSubscribes)   
    myThread.OnEnded(<- FunctionThatSubscribes)     
    myThread.OnDestroyed(<- FunctionThatSubscribes) 

    // Sending data back to main thread
    myThread.SendMain(data);

    // Pausing the thread
    myThread.End();

    // Destroying the thread (must recreate to reuse)
    myThread.Destroy();


PREBUILT FUNCTIONS
------------------

Console:
    Console.Message(message);                 // Prints a message to console
    Console.ReadConsoleInput();               // Returns last user input as string
    Console.OpenFile(filePath);               // Opens file and returns file object
    Console.CloseFile(closeAllFiles: bool);   // Closes last opened file (or all if true)
	Console.SetWindowSize(<int> width, <height> height); // set the windows size
	Console.SetCursorPosition(<int> x, <int> y); // sets the consoles cursor positionf 

Type Functions:
    <int/float/...>.ToUsableString();         // Converts number to usable string
    <string/char>.ToUsableNum();              // Converts string/char to number
    <list/array>.GetIndexOf(object);          // Finds index of first matching object
    <list/array>.AddElement(object);          // Adds object to end of list/array
    <list/array>.RemoveElement(object);       // Removes first matching objects
	<list/array>.RemoveElementAll(object);    // Removes All Instances Of That Objects

File Functions:
    <OpenedFile>.Write(data);                 // Writes data to file
    <OpenedFile>.Read();                      // Reads file as string
    <OpenedFile>.Delete();                    // Deletes file safely
    <OpenedFile>.ChangePath(newPath);         // Moves file to new location
    <OpenedFile>.Tell();                      // Returns file path
    <OpenedFile>.IsActive();                  // True if file is currently in use
    <OpenedFile>.ReadLines();                 // Reads file as list of strings
    <OpenedFile>.WriteLines(List<string>);    // Writes list of strings to file
    <OpenedFile>.IsWritable();                // True if file is writable
    <OpenedFile>.IsReadable();                // True if file is readable

Networking Functions:
    NetworkServer.SetNetworkType(type);       // Sets network type (UDP/TCP)
    NetworkServer.Start(port: int);           // Starts server on given port
    NetworkServer.Listen();                   // Listens for incoming data
    NetworkServer.Send(ip: string, broadcast: bool); // Sends data, broadcasts if true
	NetworkServer.DisconnectClient(ipOfClient);   // Disconnects specified client
    NetworkServer.Shutdown();                 // Stops server & kicks all clients
	
    NetworkConnector.Connect(ip: string, port: int); // Connects to server (error if none)
	NetworkConnector.OnShutdown(->ShutdownFunction); //
    NetworkConnector.Receive();               // Receives data as byte array
	
    NetworkUtils.DecodeMessage(data <Array Of Bytes>);         // Decodes UTF-8 data to string
    NetworkUtils.EncodeMessage(message);      // Encodes string to byte array
	
Https / Http Functions: // still bear bones still working on
	HttpClient httpClient = new();
	
	byte[] response httpClient.Request(<string> Type, <string> URL, <Byte[]> data);


CORE CODE BLOCKS
----------------

Loops:
    for (int index in <list/array>)
    {
        // Code using index
    }

    foreach (<type> item in <list/array>)
    {
        // Code using each element
    }

Conditionals:
    if (object <operator> object)
    {
        // Runs if true
    }
    else
    {
        // Runs if false
    }

Try / Exception Handling:
    try
    {
        // Code that might throw error
    }
    error(exception e)
    {
        Console.Message(e.Message);
    }
    final
    {
        // Always runs after try or error
    }

While Loops:
    while (condition == <true / false>)
    {
        // Runs until condition is oposite to input or loop broken
    }

Breaking Loops:
    for (int index in <list/array>)
    {
        if (index == 5) continue; // Skip iteration
        if (index == 8) end;      // Exit loop
    }


OPERATORS
---------
    +   // Addition
    -   // Subtraction
    *   // Multiplication
    /   // Division (float/double)
    //  // Integer division
    ++  // Increment by 1
    --  // Decrement by 1
	<   // Less then
	>   // More then
	<=  // less then or equal to
	>=  // More then or equal to
	=   // equal to
	==  // exact equal to (for statments not variables)
	**  // to the power of
